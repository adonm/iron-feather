# Toolchain pins. Activate with `mise install` (auto-installs on first run).
[tools]
just = "1"
# Optional: DuckDB CLI for poking at shard files. The app's pinned 2.0
# nightly CLI also lands in .deps/duckdb via `just setup-duckdb`.
