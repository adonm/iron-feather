apiVersion: v2
name: cachey
description: Per-zone read-through page cache for S3 (toddlers share one; pods fetch Parquet ranges over HTTP)
type: application
version: 0.1.0
# Matches values.images.tag. NOTE: tracks upstream latest; pin before prod.
appVersion: latest
