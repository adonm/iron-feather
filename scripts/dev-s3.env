# Single source for loopback-rig S3 identity. Sourced by scripts/cachey_up.sh
# (which passes it to MinIO and Cachey), scripts/lake_ref.sh and the
# lake-publish recipe. Keep in sync with charts/cachey/values.yaml
# backend.authUser/authPassword (same dev-only pair, used in kind).
S3_USER=devkey
S3_PASS=devsecret123
S3_REGION=us-east-1
